{
  "name": "truck-stop-pos",
  "version": "1.0.0",
  "private": true,
  "description": "Truck Stop POS cloud/local server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
